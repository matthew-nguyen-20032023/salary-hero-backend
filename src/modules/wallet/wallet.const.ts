export enum WalletMessageSuccess {
  GetWalletSuccess = "Get wallet successfully",
}

export enum WalletMessageFailed {
  ExecBalance = "Amount exec available wallet balance",
  InvalidReceiverWallet = "Invalid receiver wallet",
}
